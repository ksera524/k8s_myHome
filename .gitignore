# Ignore Terraform state files
*.tfstate
*.tfstate.*

# Ignore Terraform plan files
*.tfplan
tfplan

# Ignore backup files created by Terraform
*.backup

# Ignore override files, which are not meant to be version-controlled
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore sensitive variable files
*.auto.tfvars
*.auto.tfvars.json
*.tfvars
*.tfvars.json

# Ignore crash logs
crash.log

# Ignore .terraform directory (local plugin binaries and cached modules)
.terraform/
.terraform.lock.hcl

# Ignore files for Terraform CLI configuration
terraform.rc
.terraformrc

# GitHub Actions workflows (not used in this repo)
**/.github/
!/.github/

# Settings files with potential secrets
automation/settings.toml
settings.toml

# Certificate and key files
*.pem
*.key
*.crt
*.p12
*.pfx
automation/certs/

# Kubernetes config files with credentials
*kubeconfig*
*.kubeconfig
config.yaml
admin.conf

# SSH keys
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
*.ppk

# Environment files with secrets
.env
.env.local
.env.production
.env.staging
*.env

# Backup files
*.backup
*.bak
*.tmp
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# IDE files
.vscode/settings.json
.vscode/launch.json
.idea/
*.swp
*.swo
*~

# Log files
*.log
logs/
log/

# Make all log file
make-all.log

# Temporary files
temp/
tmp/
*.temp

# Docker files with potential secrets
docker-compose.override.yml
.dockerignore

# Helm values with secrets
values-prod.yaml
values-production.yaml
*-secret.yaml
*-secrets.yaml

# External Secrets definitions are safe to commit
!manifests/platform/secrets/external-secrets/*-secret.yaml
# External Secrets definitions (config/secrets) are safe to commit
!manifests/config/secrets/*-secret.yaml
# External Secrets templates are safe to commit
!automation/templates/external-secrets/*-secret.yaml
# ArgoCD config files are safe to commit
!manifests/platform/argocd-config/*-secret.yaml
!manifests/platform/argocd-config/*-secrets.yaml

# Kubernetes manifests with secrets (if any)
*-password.yaml
*-token.yaml

# Archive files (potentially containing sensitive data)
*.zip
*.tar
*.tar.gz
*.tgz
*.rar

# Known hosts files
known_hosts
known_hosts.old
