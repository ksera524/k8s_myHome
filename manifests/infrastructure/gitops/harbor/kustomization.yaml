# Harbor Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - harbor-routes.yaml
  - harbor-image-cleanup-cronjob.yaml

# ノード改変（/etc更新）が必要な場合は
# node-mutations/kustomization.yaml を明示的に適用する
