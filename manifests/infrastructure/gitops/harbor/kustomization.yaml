# Harbor Kustomization - 追加リソースのみ
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - harbor-external-ingress.yaml
  # harbor-pvc-fix-job.yaml は一旦コメントアウト（Harbor本体がHelm経由でデプロイされるまで待つ必要があるため）