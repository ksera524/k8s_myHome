# Harbor Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - harbor-complete.yaml
  - harbor-external-ingress.yaml
  - harbor-pvc-fix-job.yaml    # PostSync hookでPVC修正（念のため残す）