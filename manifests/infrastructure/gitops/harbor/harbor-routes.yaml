# Harbor外部公開用HTTPRoute（HTTP→HTTPSリダイレクト）
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: harbor-external-redirect
  namespace: harbor
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: nginx-gateway
      namespace: nginx-gateway
      sectionName: http
  hostnames:
    - harbor.qroksera.com
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
      matches:
        - path:
            type: PathPrefix
            value: /
---
# Harbor外部公開用HTTPRoute（HTTPS）
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: harbor-external
  namespace: harbor
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: nginx-gateway
      namespace: nginx-gateway
      sectionName: https-harbor-external
  hostnames:
    - harbor.qroksera.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/
      backendRefs:
        - group: ""
          kind: Service
          name: harbor-core
          port: 80
          weight: 1
    - matches:
        - path:
            type: PathPrefix
            value: /service/
      backendRefs:
        - group: ""
          kind: Service
          name: harbor-core
          port: 80
          weight: 1
    - matches:
        - path:
            type: PathPrefix
            value: /v2/
      backendRefs:
        - group: ""
          kind: Service
          name: harbor-core
          port: 80
          weight: 1
    - matches:
        - path:
            type: PathPrefix
            value: /chartrepo/
      backendRefs:
        - group: ""
          kind: Service
          name: harbor-core
          port: 80
          weight: 1
    - matches:
        - path:
            type: PathPrefix
            value: /c/
      backendRefs:
        - group: ""
          kind: Service
          name: harbor-core
          port: 80
          weight: 1
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: harbor-portal
          port: 80
          weight: 1
---
# Harbor内部公開用HTTPRoute（HTTP→HTTPSリダイレクト）
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: harbor-internal-redirect
  namespace: harbor
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: nginx-gateway
      namespace: nginx-gateway
      sectionName: http
  hostnames:
    - harbor.internal.qroksera.com
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
      matches:
        - path:
            type: PathPrefix
            value: /
---
# Harbor内部公開用HTTPRoute（HTTPS）
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: harbor-internal
  namespace: harbor
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: nginx-gateway
      namespace: nginx-gateway
      sectionName: https-internal
  hostnames:
    - harbor.internal.qroksera.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/
      backendRefs:
        - group: ""
          kind: Service
          name: harbor-core
          port: 80
          weight: 1
    - matches:
        - path:
            type: PathPrefix
            value: /service/
      backendRefs:
        - group: ""
          kind: Service
          name: harbor-core
          port: 80
          weight: 1
    - matches:
        - path:
            type: PathPrefix
            value: /v2/
      backendRefs:
        - group: ""
          kind: Service
          name: harbor-core
          port: 80
          weight: 1
    - matches:
        - path:
            type: PathPrefix
            value: /chartrepo/
      backendRefs:
        - group: ""
          kind: Service
          name: harbor-core
          port: 80
          weight: 1
    - matches:
        - path:
            type: PathPrefix
            value: /c/
      backendRefs:
        - group: ""
          kind: Service
          name: harbor-core
          port: 80
          weight: 1
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: harbor-portal
          port: 80
          weight: 1
---
# Harborクライアント設定（ボディサイズ制限解除）
apiVersion: gateway.nginx.org/v1alpha1
kind: ClientSettingsPolicy
metadata:
  name: harbor-external-client-settings
  namespace: harbor
spec:
  targetRef:
    group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: harbor-external
  body:
    maxSize: "0"
---
# Harborクライアント設定（ボディサイズ制限解除）
apiVersion: gateway.nginx.org/v1alpha1
kind: ClientSettingsPolicy
metadata:
  name: harbor-internal-client-settings
  namespace: harbor
spec:
  targetRef:
    group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: harbor-internal
  body:
    maxSize: "0"
