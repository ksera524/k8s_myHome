apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-rbac-cm
    app.kubernetes.io/part-of: argocd
data:
  # GitHub認証ユーザーへの権限設定
  policy.default: role:readonly
  policy.csv: |
    # 特定のGitHubユーザーに管理者権限を付与
    g, ksera24, role:admin