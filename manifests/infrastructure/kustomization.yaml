# Infrastructure Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # ArgoCD関連は manifests/platform/argocd-config/ に移動済み

  # Networking
  - networking/metallb/metallb-ipaddress-pool.yaml
  - networking/coredns/
