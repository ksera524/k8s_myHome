# 外部公開用ワイルドカード証明書（staging）
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-external
  namespace: nginx-gateway
spec:
  secretName: wildcard-external-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - "*.qroksera.com"
  usages:
    - digital signature
    - key encipherment
    - server auth
