apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - name: rustfs
    repo: https://charts.rustfs.com
    version: 0.0.83
    releaseName: rustfs
    namespace: rustfs
    valuesFile: values.yaml
